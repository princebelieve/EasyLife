import { useEffect, useState } from "react";
import { createTestimonialApi, deleteTestimonialApi, getAdminTestimonials, updateTestimonialApi } from "../services/api";
import { getToken } from "../utils/auth";
import useAuth from "../context/AuthContext";

const blank = { contentType: "story", title: "", name: "", role: "", testimony: "", linkUrl: "", videoUrl: "", featured: false, bannerEnabled: false, approved: true, status: "active", seoTitle: "", seoDescription: "", image: null, video: null };

export default function AdminTestimonials() {
  const [items, setItems] = useState([]);
  const [form, setForm] = useState(blank);
  const [editing, setEditing] = useState(null);
  const [message, setMessage] = useState("");
  const { isSubadmin } = useAuth();

  async function load() { setItems(await getAdminTestimonials(getToken())); }
  useEffect(() => { load().catch(() => setMessage("Unable to load testimonials.")); }, []);
  function change(event) { const { name, value, checked, files, type } = event.target; setForm((current) => ({ ...current, [name]: type === "checkbox" ? checked : files ? files[0] : value })); }
  async function submit(event) { event.preventDefault(); setMessage(""); const data = new FormData(); Object.entries(form).forEach(([key, value]) => { if (value !== null && value !== undefined && !["image", "video"].includes(key)) data.append(key, value); }); if (form.image) data.append("image", form.image); if (form.video) data.append("video", form.video); try { if (editing) await updateTestimonialApi(editing, data, getToken()); else await createTestimonialApi(data, getToken()); setForm(blank); setEditing(null); setMessage("Testimonial saved."); await load(); } catch (error) { setMessage(error.message || "Unable to save testimonial."); } }
  function edit(item) { setEditing(item._id); setForm({ ...blank, ...item, image: null, video: null }); }
  async function remove(id) { if (!window.confirm("Delete this testimonial?")) return; await deleteTestimonialApi(id, getToken()); await load(); }

  return <div className="page admin-testimonials"><div className="page-header"><div><span className="eyebrow">EASY LIFE CONTENT</span><h1>Content Posts</h1><p>{isSubadmin ? "Submit announcements, stories, outreach, journeys, or testimonies for admin review." : "Publish announcements, stories, outreach, journeys, or testimonies."}</p></div></div><form className="testimonial-admin-form" onSubmit={submit}><select required name="contentType" value={form.contentType} onChange={change}><option value="announcement">Announcement</option><option value="story">Story</option><option value="outreach">Outreach activity</option><option value="journey">Journey</option><option value="testimony">Testimony</option></select><input name="title" placeholder="Post title" value={form.title} onChange={change} /><input required name="name" placeholder="Name or organisation" value={form.name} onChange={change} /><input name="role" placeholder="Role or location (optional)" value={form.role} onChange={change} /><textarea required name="testimony" placeholder="Post content" rows="5" value={form.testimony} onChange={change} /><input name="linkUrl" placeholder="Post link (optional)" value={form.linkUrl} onChange={change} /><label>Image upload (optional)<input type="file" name="image" accept="image/*" onChange={change} /></label><label>Video upload to R2 (optional, max 100 MB)<input type="file" name="video" accept="video/*" onChange={change} /></label><input name="videoUrl" placeholder="External YouTube/Vimeo URL (optional)" value={form.videoUrl} onChange={change} /><input name="seoTitle" placeholder="SEO title" value={form.seoTitle} onChange={change} /><textarea name="seoDescription" placeholder="SEO description" rows="3" value={form.seoDescription} onChange={change} />{!isSubadmin && <><label className="wizard-checkbox"><input type="checkbox" name="featured" checked={form.featured} onChange={change} /> Feature on Home</label><label className="wizard-checkbox"><input type="checkbox" name="bannerEnabled" checked={form.bannerEnabled} onChange={change} /> Show as sitewide announcement (maximum 3)</label><label className="wizard-checkbox"><input type="checkbox" name="approved" checked={form.approved} onChange={change} /> Approved and visible</label></>}<button className="primary">{editing ? "Update post" : isSubadmin ? "Submit for admin review" : "Publish post"}</button>{editing && <button type="button" onClick={() => { setEditing(null); setForm(blank); }}>Cancel edit</button>}{message && <p>{message}</p>}</form><div className="admin-testimonial-list">{items.map((item) => <article className="testimonial-admin-item content-card" key={item._id}><div><strong>{item.title || item.contentType}</strong><span>{item.name} · {item.contentType}</span><p>{item.testimony}</p><small>{item.featured ? "Featured" : "Not featured"} · {item.bannerEnabled ? "Banner" : "No banner"} · {item.approved ? "Visible" : "Awaiting review"}</small></div><div><button onClick={() => edit(item)}>Edit</button><button className="btn-danger" onClick={() => remove(item._id)}>Delete</button></div></article>)}</div></div>;
}
